export default defineAppConfig({
  ui: {
    colors: {
      // Sober palette while the visual identity settles; final palette is a
      // design decision of the screen refonte.
      primary: 'neutral',
      neutral: 'neutral',
    },
  },
})
