// The tRPC request-context shape. Its canonical definition lives in
// `common/types/core.ts` (the composition-root types). Re-exported here so the
// tRPC plumbing has a local, discoverable reference.

export type { CoreContext } from '../../common/types/core.ts';
